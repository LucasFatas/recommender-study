class SongRating:

    def __init__(self, userId, matchedUserId, spotify_url, rating):
        self.userId = userId
        self.matchedUserId = matchedUserId
        self.spotify_url = spotify_url
        self.rating = rating

