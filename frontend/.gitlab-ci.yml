stages:
  - computation
  - build
  - jest

cache:
  paths:
    - node_modules/

install_and_build:
  stage: build
  image: node:latest
  script:
    - npm install
    - npm build
  artifacts:
    paths:
      - build/

test_application:
  stage: jest
  image: node:latest
  script:
    - CI=true
    - yarn test
  artifacts:
    paths:
      - ./