image: node:latest

cache:
  paths:
    - node_modules/

stages:
  - build
  - test

install_and_build:
  stage: build
  script:
    - yarn install
    - yarn build
  artifacts:
    paths:
      - build/

test_application:
  stage: test
  script:
    - CI=true
    - yarn test
  artifacts:
    paths:
      - ./